[general]
static=yes
writeprotect=no

[internal-agents]
; Internal extensions only. There is deliberately no outbound trunk context.
exten => _10XX,1,NoOp(Internal call ${CALLERID(num)} to ${EXTEN})
 same => n,Dial(PJSIP/${EXTEN},30,Tt)
 same => n,Hangup()
